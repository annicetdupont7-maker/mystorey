"use client";
import { useState } from "react";
import { THEME_PRESETS } from "../presets";
import { resolveStoreTheme, themeCssVariables } from "../resolve-theme";
import type { PresetId } from "../theme-schema";
import { Storefront } from "@/features/storefront/components";
import { demoProducts } from "@/features/storefront/storefront-types";
export function ThemePreview(){const [selected,setSelected]=useState<PresetId>("elegant");const {preset,tokens}=resolveStoreTheme({preset_id:selected,version:1});return <div className="vf-panel"><header style={{display:"flex",justifyContent:"space-between",alignItems:"end",gap:"1rem",flexWrap:"wrap",marginBottom:"1.25rem"}}><div><span className="vf-eyebrow">Laboratoire interne</span><h1 style={{margin:".3rem 0 0",fontSize:"clamp(1.7rem,4vw,2.6rem)"}}>L’identité d’une boutique, en direct.</h1></div><p style={{maxWidth:"31rem",margin:0,opacity:.72}}>Sept directions visuelles, un même système de composants. Cet aperçu ne collecte ni ne publie de données.</p></header><div role="tablist" aria-label="Thèmes de boutique" style={{display:"flex",gap:".5rem",overflow:"auto",paddingBottom:"1.25rem"}}>{Object.values(THEME_PRESETS).map(item=><button role="tab" aria-selected={selected===item.id} key={item.id} onClick={()=>setSelected(item.id)} style={{border:`1px solid ${selected===item.id?"#e56b52":"#d9cfc3"}`,borderRadius:999,padding:".55rem .85rem",background:selected===item.id?"#e56b52":"#fffaf3",color:selected===item.id?"white":"#202635",whiteSpace:"nowrap"}}>{item.label}</button>)}</div><div style={{display:"grid",gap:".8rem",marginBottom:"1rem"}}><strong>{preset.label}</strong><span style={{opacity:.7}}>{preset.mood}</span></div><div style={{borderRadius:"1.4rem",overflow:"hidden",boxShadow:"0 18px 60px #45321f26"}}><div style={themeCssVariables(tokens)}><Storefront tokens={tokens} products={demoProducts} whatsapp="+237 6 90 00 00 00"/></div></div><p style={{margin:"1rem 0",fontSize:".82rem",opacity:.65}}>L’aperçu est responsive : réduisez la fenêtre pour contrôler la version mobile.</p></div>}
